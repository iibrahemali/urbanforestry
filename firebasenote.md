Alert: In Firebase, the "Cloud Functions", "Storage", "Firestore", and "Realtime DB" are set as test mode in their json files. Modify that in the production phase.
However, "Authentication" should be production-ready.
